/ = form_for(@thing, url: thing_path(@thing)) do |f|
/   - if @thing.errors.any?
/     #error_explanation
/       h2
/         = pluralize(@thing.errors.count, "error")
/         | prohibited this transformation from being saved:
/       ul
/         - @thing.errors.full_messages.each do |message|
/           li
/             = message
/   .sin-form-group.sin-padding
/     .sin-condensed-form
/       .mdl-grid
/         .mdl-cell.mdl-cell--5-col.mdl-textfield.mdl-js-textfield.mdl-textfield--floating-label
/           = f.label :name, class: "mdl-textfield__label"
/           = f.text_field :name, class: "mdl-textfield__input"
/       .mdl-grid
/         .mdl-cell.mdl-cell--5-col.mdl-textfield.mdl-js-textfield.mdl-textfield--floating-label
/           = f.label :description, class: "mdl-textfield__label"
/           = f.text_field :description, class: "mdl-textfield__input"
/       .field
/         label.mdl-switch.mdl-js-switch.mdl-js-ripple-effect[for="checkbox-public"]
/           = f.check_box :public, id: "checkbox-public", class: "mdl-switch__input"
/           span.mdl-switch__label
/             | Public
/   h3
/     | Metadata
/   .mdl-grid
/     .actions.mdl-cell.mdl-cell--4-col
/       = f.submit class: "mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect mdl-button--accent"


/ = semantic_form_for @thing, url: thing_path(@thing) do |f|
/   = f.inputs
/     = f.input :name
/     = f.input :description, :input_html => {:class => 'autogrow', :rows => 10, :cols => 20}
/     = f.input :public
/   = f.actions

= simple_form_for @thing, url: thing_path(@thing) do |f|
  .mdl-grid
    = f.input :name, wrapper: :default
    = f.input :description, as: :text
    = f.input :public, wrapper: :switch
  hr.sin-invisible-form-separator
  .mdl-grid
    = f.button :submit

