<p id="notice"><%= notice %></p>

<h1>Listing Data Distributions</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @things.each do |data_distribution| %>
      <tr>
        <td><%= data_distribution.slug %></td>
        <td><%= link_to 'Show', thing_path(data_distribution) %></td>
        <td><%= link_to 'Edit', thing_edit_path(data_distribution) %></td>
        <td><%= link_to 'Destroy', thing_path(data_distribution), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Data distribution', new_data_distribution_path %>
