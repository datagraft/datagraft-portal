<%= form_for(@catalogue) do |f| %>
  <% if @catalogue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@catalogue.errors.count, "error") %> Prohibited this thing from being saved:</h2>

      <ul>
      <% @catalogue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :public %><br>
    <%= f.check_box :public %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
